# A simple program to determine the ASCII value of a character

ascii= str(input("Enter a character for which you want a ascii value: "))

# For printing the ASCII value we use a builtin keyword in python known as ord 

print("the ASCII value of the character is :", ord(ascii))
